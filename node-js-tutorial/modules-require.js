//Modules and Require()
//Other js files


//for file in current directory

//set equal to variable bc returning the counter variable value to us
let counter = require('./count');

console.log(counter(['shaun', 'crystal', 'ryu']));